package hotel.clase;

public class Factura {
    public static void genereazaFactura(String client, String camera, String data) {
        System.out.println("Factura generata pentru " + client + " la camera " + camera + " in data de " + data);
    }
}
